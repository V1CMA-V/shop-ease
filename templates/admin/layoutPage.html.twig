{% extends 'base.html.twig' %}

{% block title %}Admin Dashboard
{% endblock %}

{% block body %}
	<div class="min-h-screen flex flex-col">
		<header class="bg-white shadow-md p-4 flex justify-between items-center">
			<div class="text-lg font-bold text-blue-600">Admin Panel
			</div>
		</header>

		<div class="flex flex-1">
			<aside class="w-64 bg-white shadow-md">
				<nav class="mt-5">
					<a href="{{ path('app_dashboard') }}" class="block py-2 px-4 hover:bg-blue-100 transition-colors duration-300">Dashboard</a>
					<a href="{{ path('app_product') }}" class="block py-2 px-4 hover:bg-blue-100 transition-colors duration-300">Manage Product</a>
					<a href="#" class="block py-2 px-4 hover:bg-blue-100 transition-colors duration-300">Manage Categories</a>
					<a href="#" class="block py-2 px-4 hover:bg-blue-100 transition-colors duration-300">Manage Orders</a>
					<a href="#" class="block py-2 px-4 hover:bg-blue-100 transition-colors duration-300">Manage Users</a>
				</nav>
			</aside>

			<main class="flex-1">
				{% block content %}{% endblock %}
			</main>
		</div>


	</div>
{% endblock %}
